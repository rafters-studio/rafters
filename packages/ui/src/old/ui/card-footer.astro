---
import type { HTMLAttributes } from 'astro/types';
import classy from '../../primitives/classy';
import { type CardBackground, cardBackgroundClasses, cardFooterClasses } from './card.classes';

interface Props extends HTMLAttributes<'div'> {
  background?: CardBackground;
}

const { background, class: className, ...attrs } = Astro.props;
const bgStyles = background ? cardBackgroundClasses[background] : '';
const cls = classy(cardFooterClasses, bgStyles, className);
---

<div class={cls} {...attrs}>
  <slot />
</div>
