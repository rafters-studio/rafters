/**
 * Single source of truth for the Rafters version.
 * Used by the CLI package.json, the API root endpoint, and anywhere
 * else that needs to report the current version.
 */
export const RAFTERS_VERSION = '0.0.28';
