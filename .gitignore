node_modules
dist
build
*.log
.dev.*
.dev.vars
**/.dev.vars
.env
.env.local
.DS_Store
coverage
.npmrc.approvals
playwright-report
test-results
*.turbo/
*.astro/
.rafters/
!apps/demo/.rafters/
.scratch/
.claude/worktrees/
.playwright-mcp/
*.tsbuildinfo
# Rust
target/
*.rs.bk
# The generated token SHEETS apps import (rafters.css / rafters.ts /
# rafters.standalone.css) are tracked artifacts -- CI has no generate step, so
# a blanket ignore of output/ orphaned them and broke the lab build. Track the
# sheets; ignore the generated intermediate (rafters.json) nothing imports and
# the transient reset snapshots.
apps/demo/.rafters/output/rafters.json
apps/demo/.rafters/output/reset-*.json
