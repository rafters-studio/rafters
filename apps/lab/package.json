{
  "name": "lab",
  "version": "0.0.0",
  "private": true,
  "description": "Live dogfooding app for rafters. A real Vite + React consumer install: Tailwind v4 compiles utilities on the fly against a genuine `rafters init` token theme. Type/component toys prove the system before the registry ships them.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@rafters/ui": "workspace:*",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.1.18",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "@vitejs/plugin-react": "catalog:",
    "tailwindcss": "^4.1.18",
    "typescript": "catalog:",
    "vite": "^8.0.0"
  }
}
