#!/bin/bash
# post-compact-rafters.sh
# SessionStart hook (matcher: compact) - re-orients after compaction
# Currently a no-op. Core knowledge is in CLAUDE.md and persistent memory.

cat > /dev/null

exit 0
